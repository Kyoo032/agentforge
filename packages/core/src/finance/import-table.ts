/**
 * The spreadsheet importer's public face. It outgrew one file, so the work lives in `import-table/`
 * and this module keeps the import path every caller already uses.
 */
export * from "./import-table/index";
