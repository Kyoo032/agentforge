# 0.14.1 changelog

**Purpose (per the operator, 2026-09-07):** this file is the reference for what must be inside the 0.14.1 `setup.exe`. We develop and test on webdev (`pnpm dev`, `127.0.0.1:3000`) because it is faster, so nothing listed here counts as shipped until it is staged (`host.cjs` + renderer), packed (`pnpm desktop:build`), installed, and driven on the packaged app (`doctor --desktop`). Agents: append here after every product change; the pack step uses this as the checklist.

Append-only. Working-tree changes on `main` after the Edit platform merge (`bda18b0`, PR #21). Nothing here is committed or packed yet; the installed 0.14.0 exe (2026-09-06 19:32) predates all of it. Verify map: `.cursor/skills/verify-agentforge/features/edit.md`. Evidence: `.cursor/skills/verify-agentforge/evidence/edit/2026-09-07-*`.

## Baseline — 2026-09-07 morning

- Local `main` was one Cursor checkpoint behind `origin/main`; rebased the checkpoint (`91cd144`) onto `origin/main` so the tree has the Edit platform plus the local `.vscode` / biome / api-client tweaks. No content change from the earlier uncommitted merge (tree diff empty).
- Doctor webdev: ok, runtime `ai`, `edit.ffmpeg.found: true` (8.1.1), asr unavailable.
- Fixtures regenerated: `node scripts/edit-fixtures.mjs` → `apps/web/tests/fixtures/edit/` (gitignored mp4/png).
- Unit suites green before any change: core edit 47, host edit 46.

## Edit starter media (offline sample clips) — 2026-09-07

- **What:** the four non-blank starters seed bundled sample clips onto the timeline at create time. Fully offline; nothing fetched at runtime.
- **Core:** `packages/core/src/edit/starter-media.json` (7 files: 6 mp4 + 1 m4a, with kind/mime/duration/size/fps and a deterministic ffmpeg lavfi recipe each), `starter-media.ts` (zod loader, `STARTER_MEDIA`, `starterMediaFile`, `starterTrackFor`), `recipes.ts` (`StarterProject.description` + `media[]`; starters `blank-16x9`, `blank-9x16`, `blank-1x1`, `promo-16x9`, `talk-16x9`, `reels-9x16`, `square-1x1`; removed placeholder ids `title-music`, `subtitled-talk`), `index.ts` exports.
- **Host:** `packages/host/src/edit/starter-media.ts` (`starterMediaDir()`: packaged `resourcesPath/starters` only; dev: env `AGENTFORGE_STARTER_MEDIA_DIR` then walk-up to `apps/desktop/resources/starters`; `starterFileProblem()` rejects symlink / non-file / >200 MB / outside dir; `seedStarterMedia()` copies into `mediaRoot()/<org>/<uuid>`, inserts `media` row, ffprobe when found else manifest metadata with a `console.warn`, `add_asset` + `add_clip` sequential on v1 after any title, audio on a1 from 0; rollback of copied rows/files on failure). `projects.ts` calls it, deletes the project row if seeding throws, warns on skipped files.
- **Web:** `edit-studio.tsx` shows `edit-starter-description` under `edit-starter`.
- **Build:** `scripts/edit-starters.mjs` (renders manifest via ffmpeg; `--check` verifies presence), root scripts `edit:starters` / `edit:starters:check`, `apps/desktop/package.json` `desktop-build` runs the check first and `extraResources` copies `resources/starters` → `starters`, `stage-renderer.mjs` shells out to the check, `.gitignore` ignores the generated media, `apps/desktop/resources/starters/README.md`.
- **Tests:** core `starter-media.test.ts` (5), host `starter-media.test.ts` (5, isolated temp SQLite + temp media + fake files, ffmpeg forced missing), host `starter-media-probe.test.ts` (1, real files + real ffprobe, skips if absent).
- **Review fixes applied:** symlink/traversal guard, size cap, env override ignored in packaged builds, ffprobe fallback logs, partial-seed rollback, single presence check.
- **Proven live** (after restart): "Promo demo" → title + 3 clips v1, music a1, 21 s timeline.
- **Open:** clips are synthetic lavfi renders (gradients, test patterns), not footage. Swap any file with the same name/duration/aspect to replace.

## Edit prompt templates (cited, model-agnostic) — 2026-09-07

- **What:** 19 verbatim prompts from BytePlus ModelArk Seedance guides (1.0 pro, 1.5 pro, 2.0, 2.5, plus the BytePlus blog) and Higgsfield blog/product pages, in four categories (promo 6, talking-head 4, reel 5, square 4), plus 11 cited prompting rules. No model names in any prompt. Embedded offline.
- **Core:** `packages/core/src/edit/prompt-templates.json`, `prompt-templates.ts` (`PROMPT_TEMPLATES`, `PROMPT_GUIDE_RULES`, `PROMPT_TEMPLATE_CATEGORIES`, `promptTemplatesFor`, `promptTemplateById`), `index.ts` exports. Test `prompt-templates.test.ts` (12).
- **Web:** `apps/web/components/edit-prompt-templates.tsx` (category filter, list, citation block with link + tips, collapsible rules); `edit-generate-tab.tsx` mounts it in the Video sub-tab, picking fills prompt + snaps seconds, model select untouched, manual edit clears selection. Testids `edit-prompt-templates`, `edit-prompt-template-categories`, `edit-prompt-template-category-<id>`, `edit-prompt-template-list`, `edit-prompt-template-<id>`, `edit-prompt-template-source`, `edit-prompt-guide`.
- **Sources verified by fetch:** product-stone (Higgsfield Seedance 2.0 post), airplane UGC (Higgsfield 2.5 guide), daisy + poppies (BytePlus blog). Only page text collected; no videos downloaded. Higgsfield terms forbid scraping the service; blog content only.
- **Proven live:** reel filter → runway pan → prompt filled, seconds 5, citation with docs.byteplus.com link.
- **Layout fix (same day):** Generate button moved above the picker so it stays on screen; template list `max-h-40`. Reason: button sat at ~890 px in an 800 px window and the list swallowed wheel scroll.

## Harness / process — 2026-09-07

- `features/edit.md`: sub-features `edit-starter-media`, `edit-prompt-templates` + drive recipes.
- Dev server restarted once by the operator's request (old tree killed by PID, `npx pnpm@9.15.9 dev` relaunched). Cause: Vite's watcher does not see the linked `packages/core`, so core edits need a restart; `tsx` never reloads host code.
- Incident: a temp git worktree with symlinked node_modules was deleted with PowerShell `Remove-Item -Recurse`; it followed the links and wiped tracked files in `packages/core`, `packages/db`, `packages/university` plus part of node_modules. Restored with `git checkout --`, `pnpm install --force`, manual better-sqlite3 tarball restore. No data loss. Rule recorded: never symlink into a worktree, never recursive-delete on Windows without checking for links.

## Known, not fixed (carry to 0.14.1 decision)

- Host edit tests (15 of 17 files) write into the live `data/agentforge.sqlite`; ~100 junk projects (`import-ipc`, `gen-path`, …) plus `Blank repro`, `Blank repro 2`, `Promo demo` are in the operator's project list.
- tsc: `packages/host/src/edit/agent-run.ts` reads `scenario.args` on a `StubEditScenario | StubFillScenario` union; `backend.ts` imports `Ingredient` which the core root does not export. Runtime unaffected (`tsx`/Vite do not typecheck).
- `edit-studio.tsx` `onNewProject` falls back to a local fake project on 404, which hides a missing host route; better to show "Edit is not available in this build".
- The desktop `host.cjs` / `resources/renderer` were rebuilt from source on 2026-09-07 by an accidental full run of `stage-renderer.mjs`; both are gitignored build outputs. The installed 0.14.0 package still has no Edit routes.
- Videos studio keeps its six in-house briefs; the cited templates are Edit-only for now.

## Pack checklist for 0.14.1

1. `pnpm edit:starters` (needs ffmpeg) then `pnpm edit:starters:check`.
2. `pnpm --filter @agentforge/core test`, `pnpm --filter @agentforge/host test` (host tests still hit the live DB; run with `AGENTFORGE_DATA_DIR` pointed at a scratch dir until isolation lands).
3. Bump versions, `pnpm desktop:build`, grep the staged `host.cjs` for `starterMediaDir` and the renderer for `edit-prompt-templates`.
4. Drive `features/edit.md` "Starter media" and "Prompt templates" on the packaged app (`doctor --desktop`).
