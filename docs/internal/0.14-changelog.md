# 0.14 changelog

Append-only. Observe is the installed Agentforge window (`doctor --desktop`, `transport: "ipc"`), never `:3000`.

## phase-0 — 2026-09-06

- Loop: 1
- Guardrails: pass (`surface` Electron Chat + Settings; `doctor` ipc)
- Files: none (baseline)
- Pack: none
- Grep installed `app.asar` `host.cjs`: `sanitizeGatewayRequestBody` miss; `store: false` miss; `armStreamWatchdog` miss (watchdog helpers present as other names); `provider.zdr` hit (OpenRouter helper); `reasoningEffort` hit
- Grep installed renderer: `submitOnEnter` miss; `WorkModeKeepAlive` miss; `thinking-toggle` / `reasoning-effort` hit; `key-fingerprint` hit; `chat-usage` hit; leftover debug ingest to `127.0.0.1:7839`
- Doctor --desktop: `transport: ipc`, `productName: Agentforge`, `dataDir: C:\Users\rizky\AppData\Roaming\Agentforge`, `pid: 28052`, `runtime: ai`, `hasOpenai: true`, `gatewayName: Toko Token`
- ProductVersion: `0.13.5.0`
- Electron drive:
  - Chat (`evidence/0.14/phase-0/chat-forced.png`): empty Chat, GPT 5.6 Luna, effort **None**, `chat-usage` `$0.02 used · $29.98 left`, context `1.05M left`
  - Settings (`evidence/0.14/phase-0/usage.png`): Status Live, key saved, `key-fingerprint` `sha256:60fd883c47ac`, this-key `$0.02 used · $29.98 left`, install line **0.13.5**, updates 404 on `releases.atom`
  - Enter: not exercised in composer (rail click stayed on Settings). Installed renderer has no `onKeyDown` / `submitOnEnter` — Enter does not send
- Human approval: pending (execute-all requested; evidence attached)
- Not in this pack: all 0.14 wires (sanitize, Enter, keep-alive, `store: false`, version bump)

## phase-1 — 2026-09-06

- Loop: 1 (landed in the 0.14.0 pack with phases 2–5)
- Guardrails: pass — staged/installed `host.cjs` contains `omitted temperature`, `must be omitted`, `status_code=`
- Files: `request-constraints.ts(+test)`, `ai-sdk-runtime.ts` `wrappedFetch` sanitize + fail-closed HTTP, `runs.ts` always emit `run.failed`, `chat-session.tsx` `onFailed` clears running
- Pack: `Agentforge Setup 0.14.0.exe`
- Grep installed host: `omitted temperature` HIT; `store=!1` HIT
- Doctor --desktop: ipc, pid 26076, ProductVersion `0.14.0.0`
- Electron drive: Chat of 0.14 (`evidence/0.14/phase-5/launch.png`). Did not fire a live Claude 400 this pass (would spend the key). Sanitize is in the installed host.
- Human approval: pending
- Not in this pack: n/a (same 0.14.0 exe)

## phase-2 — 2026-09-06

- Loop: 1
- Guardrails: pass — installed renderer `index-Dw259vmu.js` contains `key!=="Enter"` + Shift/Alt/Ctrl/Meta guard and `absolute inset-0 min-h-0 overflow-hidden` (keep-alive)
- Files: `composer-enter.ts(+test)`, `chat-composer.tsx`, `job-regen-panel.tsx`, `work-mode-keep-alive.tsx`, `App.tsx`
- Electron drive: Chat window proven. SendKeys did not land in the composer (another OS window stole focus after click). **Kyo: press Enter in the open 0.14 Chat to confirm send.**
- Human approval: pending

## phase-3 — 2026-09-06

- Loop: 1
- Guardrails: pass — installed host `r.store=!1` + `delete r.previous_response_id`; `zdr:!0` only via OpenRouter helper; Toko URL tests do not add `provider.zdr`
- Files: `applyZeroRetention`, `wrappedFetch` order retain → zdr → sanitizer; `shouldRetryWithoutTools` skips temperature 400
- Unit: 32 core + 5 composer-enter tests green
- Electron drive: not a live mid-chat switch this pass
- Human approval: pending

## phase-4 — 2026-09-06

- Loop: 1
- Guardrails: pass — no renderer hash; IPC settings already returned fingerprint + usage
- Electron drive (0.13.5 baseline, still true on 0.14 Chat): `chat-usage` `$0.02 used · $29.98 left`; Settings `key-fingerprint` `sha256:60fd883c47ac`
- Files: none required beyond existing host payload
- Human approval: pending

## phase-5 — 2026-09-06

- Loop: 1
- Guardrails: both `apps/desktop/package.json` version fields `0.14.0`; artifact `Agentforge Setup 0.14.0.exe` (spaces); `latest.yml` `path` + `url` use spaces
- Correction (2026-09-07): the "latest.yml path + url use spaces" guardrail was wrong; electron-builder's hyphenated names are correct and the pack-brand rewrite was removed.
- Files: version bump, `pack-brand.mjs` latest.yml rewrite, verify maps `chat.md` / `settings.md`
- Doctor --desktop: `transport: ipc`, `productName: Agentforge`, pid 26076
- ProductVersion: `0.14.0.0`
- Electron drive: Chat of installed 0.14 (`evidence/0.14/phase-5/launch.png` + `typed.png`); usage chip live. Enter/Claude/switch still need a human press in that window.
- Human approval: pending
- Not in this pack: flavors, `gh release`, reasoning-effort UI change (already on 0.13.5 / #20)

## ship-vs-desk — 2026-09-06 19:14 WIB

Honest ledger after Kyo asked whether later work is actually in 0.14.

**Git:** `HEAD` is still `8f8a16b` (0.13.5, #20). No 0.14 commit exists. `apps/desktop/package.json` on disk says `0.14.0` but that bump is uncommitted.

**Installed window (trust this, not git):**
- `C:\Program Files\Agentforge\Agentforge.exe` ProductVersion `0.14.0.0`, mtime `2026-09-06 18:05:48`
- Renderer `index-CRbJR4-_.js`
- Doctor `--desktop`: ipc, Toko Token, live key

| In the 18:05 exe | Proof |
|---|---|
| Enter sends | renderer `key!=="Enter"` HIT; CDP Enter drive |
| Keep-alive | renderer `absolute inset-0 min-h-0 overflow-hidden` HIT; Chat↔Documents draft held |
| IPC abort / Running can clear | renderer `abortStream` HIT; asar `host:stream-abort` HIT |
| Sanitize / fail-closed HTTP | asar `omitted temperature` + `must be omitted` + `status_code=` HIT |
| Zero-retention field drop | asar `previous_response_id` HIT (`store:!1` string miss — minified) |
| Watchdog copy | asar `No stream events from` HIT |
| Usage + fingerprint UI | renderer `chat-usage` + `key-fingerprint` HIT; driven |
| Live generate | test key saved; Flash + Luna `message-output` **pong** (not :3000) |

| Not in the 18:05 exe (source only, after that pack) | Where |
|---|---|
| `1st try` / `2nd try` / `3rd try` / `run.probing` | renderer + asar MISS |
| `coerceReasoningEffortForModel` (Luna `none`→`low`) | asar MISS |
| `readHttpErrorBody` (403 body timeout) | asar MISS |
| Probe scripts / WinApp harness / `.vscode` | never product |

**Trust note:** 0.14 was packed from a dirty tree, then more source landed without another install. Anything after 18:05 is not in the window Kyo is running. Do not treat a source edit as 0.14 until the exe is rebuilt and those strings grep HIT.

## how — Chat send (pstack) — 2026-09-06

Moved and refreshed: [`maps/chat-send.md`](maps/chat-send.md). The 2026-09-06 text described the 0.14 shape and is stale on the watchdog budgets, the testid names and the reasoning-effort path.

## pack-after-18:05 — 2026-09-06 19:35 WIB

Product source that was missing from the 18:05 exe (desktop-bridge / ipc-abort / Enter / keep-alive already HIT then):

- `run.probing` + probe labels (literals `Probing`, `1st`/`2nd`/`3rd` — not a contiguous `1st try` string)
- `coerceReasoningEffortForModel` (asar `luna|sol|terra`)
- `readHttpErrorBody` (asar `8e3`)

Also this pack: `@agentforge/core/retry` subpath so Vite does not pull Node `crypto` into the renderer.

**Pack:** `apps/desktop/dist-014b/Agentforge Setup 0.14.0.exe` (19:32). Installed over Program Files. Renderer `index-C_14QwVA.js` mtime 19:32:10.

**Grep Program Files (HIT):** renderer `run.probing` `Probing` `1st` `2nd` `3rd` `abortStream`; asar `run.probing` `luna|sol|terra` `8e3` `host:stream-abort`.

**Doctor --desktop:** ipc, pid 20644, Toko Token, live key. URL `file:///C:/Program Files/Agentforge/resources/renderer/index.html#/chat`.

**Drive `chat-probe`:** Send `VERIFY probe` → `composer-send` `1st try…`, `thinking-placeholder` `Probing gpt-5.6-luna · 1st try`, then `message-output` `pong`. Evidence `.cursor/skills/verify-agentforge/evidence/0.14/pack-after/`. 2nd/3rd try not shown — first contact succeeded.

**Map:** `features/chat.md` `chat-probe` + `chat-fail-closed`; `features/desktop.md` ship-vs-desk. Pstack how: composer → IPC → `startModalityRun` → `run.probing` → `streamText`.

Harness only (never ship): verify scripts, `.vscode`, `biome.json`. No commit.

