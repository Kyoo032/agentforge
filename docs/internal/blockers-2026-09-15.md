# Blockers and security follow-ups — 2026-09-15

Branch `cursor/id-locale-harness-chat-layout`, uncommitted (123 modified + 40 untracked at time of writing). Written by the orchestrating session after the Finance investigation; every item names the file to open and the proof required to close it. Update this file in place; move closed items to the bottom with the date.

Rules that still apply to every item: nothing is committed until Kyo asks; never commit `.env`, `settings.enc`, `.master-key`, or anything under `data/`; live drives run on a throwaway `AGENTFORGE_DATA_DIR` (path in `$TEMP/agentforge-drive-dir.txt`) with `PORT=3100`, never on the operator's desk; the test key lives only in that throwaway `settings.enc`.

## 1. Product blockers before the 0.14.26 cut

**All six (P1–P6) closed 2026-09-15.** The rows moved to [Closed](#closed) with their proof. Nothing product-side is blocking the 0.14.26 cut; what is left is verification (section 2) and the open security items in section 3.

**P7–P16 opened 2026-09-15 by the pstack mapper pass** — found while writing [`maps/`](maps/README.md), **not fixed** (the mapping run is read-only). None of them blocks the 0.14.26 cut, which is already published; they are the backlog that pass produced. Full write-up with the same citations in [`unreleased.md`](unreleased.md#findings-from-the-harness-pass--2026-09-15).

| # | Finding | Severity | Status | Close when |
|---|---|---|---|---|
| P7 | **Chat swallows `403 gateway_blocked`.** The host answers a flat body whose `error` is the *string* `"gateway_blocked"` (`packages/host/src/errors.ts:20-25`), but the composer reads `payload.error?.message ?? t("chat.error.runFailed")` (`apps/web/components/chat-composer.tsx:244-246`), and `.message` on a string is `undefined`. `parseGatewayBlocked` (`apps/web/lib/gateway-gate.ts:134`) is wired only into `settings-page.tsx` / `onboarding-screen.tsx`. A user whose key went invalid sees "run failed" and no reason. Map: [`maps/chat-send.md`](maps/chat-send.md). | **HIGH** — user-visible; the recovery path is unreachable from where the user hit the wall | open | The composer's `!response.ok` branch runs the body through `parseGatewayBlocked` / `gatewayReasonKey` and renders the `onboarding.gate.*` copy, with a test that a 403 `gateway_blocked` body puts the invalid-key string in `composer-error` rather than `chat.error.runFailed`. |
| P8 | **Renderer stream watchdog never leaves first-token mode.** `readSse` calls `dog.touch()` only (`apps/web/components/chat-composer.tsx:254`, `:314`); `touchOutput()` is never called on this path and `streaming` flips only there (`packages/core/src/runtime/stream-watchdog.ts:168`), so by `streamWatchdogDeadline` (`:88-97`) the client deadline stays `startedAt + ttfbMs` for the whole run. A long, actively-streaming answer can be aborted client-side while tokens are visibly arriving. The host-side guards do call `touchOutput`, so the asymmetry is renderer-only. Map: [`maps/chat-send.md`](maps/chat-send.md). | **HIGH** — user-visible; silently truncates good long answers, and the message is a timeout the user cannot act on | open | `readSse` calls `touchOutput()` for output-bearing events (`assistant.delta`, `assistant.thinking`, `tool.*`) and `touch()` for the rest — the same split as `isRunOutputEvent` (`packages/host/src/run-stall.ts`) — with a test that a stream still emitting deltas past the TTFB budget is not aborted. |
| P9 | **Probing has no UI and its copy is unreachable.** `onProbing` is declared (`apps/web/components/chat-composer.tsx:33`) and never referenced anywhere else in the repo; `run.probing` is parsed then reduced to `onStarted?.()` (`:131-133`). `chat.error.couldNotReach` exists in both catalogs (`apps/web/locales/{en,id}/chat.json:114`) with zero call sites, yet `features/chat.md` promises that copy after 3 failed tries. Map: [`maps/chat-send.md`](maps/chat-send.md). | MEDIUM — the verify recipe promises behaviour the code cannot produce | open | Either `ChatSession` passes `onProbing` and surfaces attempt state plus `chat.error.couldNotReach` after the last attempt, with a stub-driven test; or the prop, both locale keys and the `features/chat.md` clause are deleted together. |
| P10 | **`injectionGuardBypass` leaves no audit line.** The scan is skipped outright — `bypass ? null : scanInjection(...)` (`packages/host/src/knowledge.ts:409-415`, `packages/host/src/knowledge-ingest.ts:79-85`) — with no log and no mark on the source row; only real blocks warn. No test exercises the bypass on the knowledge path. Map: [`maps/knowledge-ingest-loop.md`](maps/knowledge-ingest-loop.md). | MEDIUM — security posture: after the fact, an unscanned source is indistinguishable from a scanned one | open | An unscanned ingest is recorded — a `console.warn` at minimum, ideally a flag on the source row surfaced in `knowledge-source-row` — plus a test that ingest with the bypass on indexes the source **and** marks it unscanned. |
| P11 | **Uploaded `.html` keeps its raw markup.** `sourceKind()` routes every `text/*` mime, `text/html` included, to the raw UTF-8 branch (`packages/host/src/knowledge-extract.ts:44-60`, `:181-182`), unlike URL ingest which always runs `htmlToText` (`packages/host/src/knowledge.ts:481-483`). Tags, scripts and attributes become chunk text and retrieval surface. Map: [`maps/knowledge-ingest-loop.md`](maps/knowledge-ingest-loop.md). | MEDIUM — injection surface and retrieval quality; a saved web page is a normal thing to upload | open | `sourceKind()` routes `text/html` (and `.html` / `.htm` by extension) through `htmlToText` like the URL path, with a test that an uploaded HTML file indexes visible text only and no tag names. |
| P12 | **`common` i18n namespace has no parity test.** Every other namespace in the list at `apps/web/lib/i18n.ts:39-58` has an en/id key-set assertion; `common` — holding `restartApp`, `restartHint`, `language`, `english`, `bahasa` — has none. Map: [`maps/locale-boot-and-run-harness.md`](maps/locale-boot-and-run-harness.md). | LOW — coverage gap, on the surface that switches the locale | open | A parity test covers `common` — its own file, or an added namespace in `apps/web/lib/settings-locales.test.ts` — and fails on a key present in one catalog only. |
| P13 | **Nothing pins that the Bahasa Indonesia rule reaches a built system prompt.** `withOutputLanguage` (`packages/core/src/output-language.ts:66-76`) is wired into eight harnesses (`packages/host/src/document-generate.ts:90`, `finance-generate.ts:226`, `data-generate.ts:250`, …), but coverage is `packages/core/src/output-language.test.ts` (catalog parity, idempotence) plus `packages/host/src/job-regen.test.ts:74-81` (the runtime `locale` field only). A grep of the per-harness `*-generate.test.ts` files for `locale` returns nothing. Map: [`maps/locale-boot-and-run-harness.md`](maps/locale-boot-and-run-harness.md). | MEDIUM — the model-facing half of the 0.14.26 locale sweep is unprotected against regression | open | At least one assertion per harness that its built system prompt carries the id rule when the run locale is `id` — e.g. `documentJobSystemPrompt(false, "id")` matches `/Bahasa Indonesia/` — covering all eight `withOutputLanguage` call sites plus Chat, Presentation, Legal, Images and Market's own mechanisms. |
| P14 | **A refused relaunch soft-applies the locale without telling the user.** When `relaunchDesktopApp()` returns `ok:false` (webdev, installing an update, already exiting, untrusted sender) the Restart button dispatches `LOCALE_RESTART_EVENT` as a fallback (`apps/web/components/settings-page.tsx:257-278`, comment `:268-269`) and the chrome re-renders in the new language. The host's boot locale moved only if the preceding `apply-locale` → `applySavedLocaleAsBoot()` (`packages/host/src/locale-boot.ts:26-29`) succeeded, so generated output can stay in the old language while the UI shows the new one. Map: [`maps/locale-boot-and-run-harness.md`](maps/locale-boot-and-run-harness.md). | MEDIUM — silent half-applied state that presents as success | open | The refused-relaunch path keeps the restart-needed notice instead of presenting as done, and a test asserts that when `relaunchDesktopApp` reports `ok:false` the UI leaves `settings-locale-restart` visible (or renders an equivalent "restart still needed" row) rather than clearing it. |
| P15 | **`localeForRun()` is Chat-only in practice.** It is `getRunContext()?.locale ?? getBootLocale()` (`packages/host/src/run-context.ts:21-23`), and the only product `withRunContext` call is Chat's (`packages/host/src/runs.ts:280`); the sole other occurrence is `packages/host/src/job-regen.test.ts:75`. Every job harness resolves the frozen boot locale and the `RunContext` branch is dead for them. Correct today; a trap for whoever adds a per-run locale to a job body. Map: [`maps/locale-boot-and-run-harness.md`](maps/locale-boot-and-run-harness.md). | LOW — no user-visible defect today; latent | open | Either job routes wrap their work in `withRunContext` so a future per-run locale is honoured everywhere, or `run-context.ts` carries a comment stating the store is Chat-only and jobs read the boot locale by design — whichever, with the decision recorded in `maps/locale-boot-and-run-harness.md`. |
| P16 | **Host drops an already-local tool video.** `isHttpOrDataUrl` allows the `/api/v1/media/` prefix for images only (`packages/host/src/tool-media.ts:3-14`), so a tool returning an already-local video path fails the predicate at `:35` and is dropped by `collectToolMediaParts` before `mirrorToolMediaPart` (`packages/host/src/runs.ts:348`, `:450`) — which would have passed it straight through. Observed from source, not confirmed against a live case. Map: [`maps/renderer-media.md`](maps/renderer-media.md). | LOW — no live repro yet; a tool that returns a local video path silently loses it | open | `isHttpOrDataUrl` accepts `/api/v1/media/` for `kind === "video"` as it already does for images, with a test in `packages/host/src/tool-media.test.ts` that a `success: true` output whose `video` is a local media path yields a `video_url` part. |


## 2. Verification owed (release confidence)

| # | Item | Proof |
|---|---|---|
| V1 | Packaged Windows drive of Start over + gateway gate. Nothing on this branch has been packed. | `doctor --desktop` ok; `host-status.json` `hasOpenai: true` at boot on a desk with a key (the boot read now resolves the desk via `GET /api/v1/context`, `apps/desktop/main.cjs:522-539`); `hasOpenai: false` after `settings-reset-key-submit`; `settings-reset-all-submit` relaunches into onboarding. |
| V2 | Mac launch smoke on real hardware. Builds pass `verify-bundle.py` for 0.14.23 and 0.14.25; never launched on a Mac. | `apps/desktop/platform/macos/AGENTS.md` steps. |
| V3 | Legal live matter run (open since 0.14.23). | `features/legal.md` steps 4 to 8. |
| V4 | Host vitest isolation from the operator's desk. `router.test.ts` was fixed today (own `mkdtempSync`); a general vitest setup file is still missing, and a run today wrote `data/workspace-id.txt` (now gitignored, harmless). | setup file sets `AGENTFORGE_DATA_DIR` / `MEDIA_ROOT` per file; `settings-desk-scope.test.ts` stays green. |
| V5 | Chat-side stall guard cannot abort the upstream request (pre-existing, amplified): the user sees `run.failed` while the gateway keeps streaming and billing until the core watchdog fires. | plumb an abort signal through `runtime.execute` or document the gap in `runs.ts`. |
| V6 | Packaged CSP never smoke-tested on `win-unpacked`. The policy was proven on a real `file://` Electron window with a Vite-shaped page, and the meta tag is the load-bearing half (`webRequest` does not observe Chromium's file loader — see E13), but nobody has run `desktop-build` and launched the packed app since. | CSP `<meta http-equiv>` present in `resources/renderer/index.html` of the packed output, and a drive of the packed app with zero CSP violations in the renderer console. |
| V7 | `electronFuses` deferred until electron-builder 26. 25.1.8 has no such key (absent from `app-builder-lib@25.1.8`'s `scheme.json`), so `runAsNode` / inspect / cookie-encryption / asar fuses are unavailable. | the 26.x bump lands and the fuses are set in the isolated NSIS worktree; `enableEmbeddedAsarIntegrityValidation` stays `false` while the Windows flow is unsigned (E21/E22). |

## 3. Security

### 3.1 Known today (from this session's reviews)

| # | Finding | Where | Severity | State |
|---|---|---|---|---|
| S1 | Gateway error passthrough to the UI (see P4). Any verbatim upstream text is also a phishing / instruction-injection surface. | `packages/core` gateway error parsing, `finance-studio.tsx` error line | MEDIUM | fix queued |
| S2 | `POST /api/v1/settings/reset` is destructive over loopback HTTP on webdev. IPC sender-frame check exists when packaged (`apps/desktop/main.cjs` `app:relaunch`); browser-tab CSRF against `127.0.0.1:3000` needs a verdict (Origin/Host check in `apps/web/server.ts` / `packages/host/src/http-adapter.ts`). | `packages/host/src/handlers/settings.ts`, `apps/web/server.ts` | HIGH if no origin check | audit running |
| S3 | `gateway-gate.json` stores a per-key fingerprint. Confirm the fingerprint is salted / not reversible and that the key never appears in logs, error bodies, or `settingsPayload()`. | `packages/host/src/gateway-gate.ts` | MEDIUM | audit running |
| S4 | A private-repo GitHub URL was shipped in the media-pricing payload on `GET /api/v1/images` / `/videos`; removed today (`repo:` marker). Confirm no other internal URL or hostname ships in API responses or docs. | `packages/core/src/models/media-pricing.ts` | LOW | fixed; sweep running |
| S5 | Reset wipe refuses any dir but `localDataDir()` and uses named entries only (`HOST_RESET_ENTRIES`), after an incident where a worker's intermediate wipe deleted the operator's webdev SQLite. Symlink/realpath and TOCTOU between marker write and boot apply still need a written verdict. | `packages/db/src/reset.ts` | MEDIUM | audit running |
| S6 | Test key hygiene: no key-shaped strings in the diff or untracked files (verified 2026-09-15). The key exists only in the throwaway desk's `settings.enc`. | — | — | delete the throwaway dir after the session; rotate the key if it is ever used outside local drives |
| S7 | Request-scoped desk ids (`WORKSPACE_COOKIE`) are no longer promoted to the machine-wide selection (`tenant.ts:27-38`), but validation of the id's characters before it reaches `workspace-id.txt` and per-desk scoping of every query still need a verdict. | `packages/host/src/tenant.ts`, `workspace.ts`, `http-adapter.ts:229` | MEDIUM | audit running |

### 3.2 Whole-codebase audit (committed + working tree), 2026-09-15

Four security reviewers were dispatched; their findings are appended below as they report. Surfaces: (a) this branch's diff; (b) host API, db, secrets; (c) Electron shell, packaging, auto-update; (d) renderer, gateway client, cloud/portal, repo hygiene.

#### (a) This branch's diff — reviewed 2026-09-15

| # | Finding | Severity | State |
|---|---|---|---|
| a1 | `saveGateState` wrote `gateway-gate.json` with no `try`/`catch`, so EACCES / ENOSPC / a directory on the path threw out of the post-save gate refresh and the owner was told "saving your key failed" for a key that **was** saved. | HIGH | fixed |
| a2 | The gate fails open by design (`deriveGatewayGate` opens on a missing or unreadable verdict; the background re-check swallows failures), so `allowed` is a UX signal, not an entitlement check. Behaviour deliberately unchanged. | HIGH | documented |
| a3 | `.gitignore` did not cover `data/datasets/`, `data/legal/`, `data/*.tmp` or `data/host-status.json`, so generated desk content could be committed. | MEDIUM | fixed |
| a4 | The IPC sender-frame check guarded only `app:relaunch`; every other `ipcMain` handler accepted any frame. | MEDIUM | fixed |
| a5 | Mutating `/api` accepted a missing `Origin` as same-machine, never checked `Host`, and the destructive reset routes were reachable by a plain cross-site form POST. | MEDIUM | fixed |
| a6 | `reset.ts` removed `target` rather than the realpath-checked `real`, and `removeDatabaseElsewhere` passed `recursive: true` without checking the sqlite path is a regular file. | LOW | fixed |
| a7 | `gateway-http-copy.ts` passed an upstream gateway `detail.message` through to the UI unredacted. | LOW | fixed |
| a8 | `handleCancelReset` used `rmSync(..., { force: true })`, so a directory at the marker path turned "call the wipe off" into a 500 with the wipe still queued. | LOW | fixed |
| a9 | The media pricing helpers were keyed on the owner-supplied `settings.openaiBaseUrl` instead of the pinned gateway base URL. | LOW | fixed |
| a10 | The ffmpeg / ffprobe *probes* (`execVersion`, `whichOnPath`) spawned a binary found on PATH with the host's full `process.env`, wrap key included — only the real runs used `minimalEnv()`. The denylist loop in `ffmpeg/run.ts` that looked like it covered this was dead code. | LOW | fixed |

**Backlog (not fixed on this branch):**

- `sandbox: true` on the renderer, which first needs brand/logo loading moved off the preload and onto IPC.
- Renderer CSP — landed under (c) for the packaged renderer; webdev is deliberately left without one.
- Rate limiting on the local HTTP surface, and a size/count budget for streaming multipart uploads.
- Verify `docs/internal/` is excluded from the public releases repo (`Kyoo032/DPS-Agent-Platform`) before the next cut.
- GET routes with side effects: `GET /api/v1/settings` fires the background gateway refresh, and the tenant path writes `workspace-id.txt`. Safe methods should not write.
- `X-Content-Type-Options: nosniff` on the byte branch of `packages/host/src/http-adapter.ts` (next to the `Cache-Control` line in the `result.type === "bytes"` block) — carried over from the earlier pass, still owed.

#### (b) Host API, db, secrets — reviewed 2026-09-15

Thirteen findings. Eight fixed on this branch, five open and carried as backlog. The fixed rows are the ones written up under "Security pass" and the nosniff item in [`0.14.26-changelog.md`](0.14.26-changelog.md).

**Fixed this pass**

| # | Finding | Severity | State |
|---|---|---|---|
| b1 | **The gateway key could be sent to an owner-chosen host.** `resolveProviderKeys` pins `openaiBaseUrl`, but call sites read the stored value raw while sending `Authorization: Bearer <gateway key>` — `account-usage.ts` (this-key state and the pricing catalog), `knowledge-embed.ts` (`POST <base>/embeddings`), `selectable-models.ts` (`detectCompatibleApi`), plus the provider-shape read in `runs.ts`. Anything that ever wrote an `openaiBaseUrl` into `settings.enc` could collect the key. | CRITICAL | fixed — all five reads now go through `resolveProviderKeys(settings).openaiBaseUrl`; `handlers/settings.ts` no longer accepts `openaiBaseUrl` in the patch body at all; `withGatewayDefault` in `settings-store.ts` discards a stored endpoint that is not the pinned URL on both the load and the save path, warning once without naming the URL. Guard `packages/host/src/gateway-pin.test.ts` greps `packages/host/src` + `packages/core/src` for raw `settings.openaiBaseUrl` reads with an allowlist that must stay empty, and asserts the handler never reads `body.openaiBaseUrl`; `settings-endpoint.test.ts` rewritten from "a custom endpoint is stored" to "the endpoint is pinned". |
| b2 | **No `Host` check on the local API.** Mutating `/api` accepted a missing `Origin` as same-machine and never looked at `Host`, so DNS rebinding and a plain cross-site form POST both reached destructive routes. | HIGH | fixed — a loopback `Host` is now required on every mutating request (a *missing* Host is refused, unlike a missing Origin: HTTP/1.1 makes it mandatory and `pathnameOf` would otherwise default it to `127.0.0.1`), and the reset routes additionally require the `x-agentforge-transport` header that `apiFetch` stamps in one place. GETs byte-for-byte unchanged. |
| b3 | **.docx zip bomb in Legal.** `parseDocxOrThrow` in `legal/store-files.ts` called `readDocx` straight, so a few hundred bytes of archive declaring gigabytes of XML inflated unchecked. | MEDIUM | fixed — reuses Knowledge's `readDocxUnderCaps` (byte cap, declared-inflated-size pre-scan, 20 s deadline), exported for the purpose from `knowledge-extract.ts`. Test `packages/host/src/legal/docx-caps.test.ts` ("refuses a zip that declares more inflated bytes than the cap, before inflating it"), which also keeps a real .docx readable and non-Word bytes reported as unsupported. |
| b4 | **Cross-desk IDOR on the Edit routes.** `handlePostEditUndo`, `handlePostEditKeep`, `handleGetEditJob`, `handlePostEditJobCancel`, `handleGetEditExportFile` and `handleGetEditEvents` called `getTenant()` and then used the path ids without folding the project against the caller's workspace — the events stream made no tenant call at all — so desk B could rewind, cancel, download the export of, and watch every op on desk A's project. | MEDIUM | fixed — each folds the project first, and the three job routes go through a new `jobInProject` helper that also pins the job to the project in the path. Test `packages/host/src/edit/cross-desk.test.ts`, eight cases: 404 for desk B on each of the six routes, the owning desk still served, and a job from a sibling project on the same desk refused. |
| b5 | **Dependency advisories.** `pnpm audit --prod` reported 3 high / 1 moderate / 2 low. | MEDIUM | fixed — pnpm `overrides` in the root `package.json` lift `jsondiffpatch` (via `ai@4.3.19`) to `>=0.7.6` and `image-size` (via `pptxgenjs`) to `>=2.0.3`; there is no fixed 1.x, so this is a major bump of a transitive dependency. Six advisories down to **2 low**: `ai@<5.0.52` and `@ai-sdk/provider-utils@<3.0.28`, both of which need the **AI SDK v5 migration** and cannot close before it. |
| b6 | `X-Content-Type-Options: nosniff` was missing on every host response, so a byte response with an attacker-influenced content type could be sniffed into something executable. | LOW | fixed in the 0.14.26 prep commit — `writeHostResult` sets it on the JSON, `bytes` (next to `Cache-Control`) and SSE branches; assertion in `http-adapter.test.ts`. |

**Open — backlog**

| # | Finding | Severity | State |
|---|---|---|---|
| b7 | **The loopback API is unauthenticated on webdev.** Origin/Host/transport checks stop a cross-site *browser*, but any local process that can open `127.0.0.1:3000` speaks to the host with the owner's authority — no token, no per-caller identity. Packaged builds use IPC and are not exposed this way. | HIGH | **open** → boot token: the server mints a per-boot token, the renderer receives it at bootstrap and sends it on every call, and the host refuses a call without it. Not started. |
| b8 | **No rate limit and no spend cap.** Nothing throttles `/api` and nothing caps how much a run (or a loop of runs) can spend against the gateway key. A stuck client or a prompt-injected agent loop bills the owner until they notice. | HIGH | **open** — needs both a request budget on the local surface and a per-run / per-day token or currency cap with a visible counter. |
| b9 | **Media is org-scoped, not desk-scoped.** Media rows resolve by organisation rather than by workspace, so a second desk on the same install can read the first desk's generated media. The Edit IDOR (b4) was the same class, fixed there; media has not been done. | MEDIUM | **open** — fold `workspaceId` into the media lookups the way `jobInProject` folds the project. |
| b10 | **The SSRF guard is name-only.** `assertAllowedEndpointUrl` and friends check the *hostname* against an allowlist; they do not resolve the name and check the address, so a DNS answer pointing at `169.254.169.254` or a loopback/private range still passes, and the name can be re-resolved between check and fetch. `fetchPublicHttps` (used by the media mirror, b/d overlap) re-checks every redirect hop but on the same name-only basis. | MEDIUM | **open** — resolve, check the resolved address family and range, and pin the connection to the checked address. |
| b11 | **Model-chosen URL exfiltration.** A tool call whose URL the model composes can carry conversation content out in the query string to any host the name-only guard lets through. The renderer half was closed under (d) (host-served media only); the host half is a matter of what the model is allowed to reach at all. | MEDIUM | **open** — pairs with b10; needs an egress allowlist for model-supplied URLs rather than a denylist. |
| b12 | **`injectionGuardBypass` is one POST away.** The prompt-injection guard can be turned off through a single settings write; there is no confirmation, no audit line, and nothing re-enables it. Combined with b7 (any local process can make that POST) it is a one-call disarm of the guard. | MEDIUM | **open** — at minimum make it a confirmed, logged, per-session choice rather than a persisted boolean. |
| b13 | **`.master-key` sits beside the ciphertext, and the KDF is a bare SHA-256.** The wrap key file lives in the same data directory as `settings.enc`, so anything that can read the folder has both halves; and the key derivation is a single SHA-256 pass rather than a memory-hard KDF, so a stolen pair is cheap to attack offline. Windows also has no OS keyring binding here (keytar holds the wrap key; see E25). | LOW | **open** — move to a memory-hard KDF (scrypt/argon2) and bind the wrap key to the OS keystore (`safeStorage`) so the two halves are never co-located. |

**Also confirmed clean in this pass:** the gate's per-key fingerprint never appears in `settingsPayload()`, logs or error bodies (S3); the reset wipe is a named entry list checked against `localDataDir()` real paths (S5); and request-scoped desk ids are no longer promoted to the machine-wide selection (S7) — character validation before `workspace-id.txt` is folded into b9's scoping work.

#### (c) Electron shell, packaging, auto-update — reviewed 2026-09-15

Twenty-five findings — not the eighteen the dispatch note guessed at. Eleven were fixed on this branch, nine come out of the shell review, and five are packaging observations read straight out of `package.json` / `main.cjs` while settling the fuses question (they are observations, not a packaging review).

**Fixed this pass**

| # | Finding | Severity | State |
|---|---|---|---|
| E1 | Every `ipcMain` channel (`host:request`, `host:save-bytes`, `host:stream-abort`, `agentforge:pick-media`) accepted a call from any frame — only `app:relaunch` checked the sender. A subframe or a devtools page could dispatch into the host with the owner's authority. | HIGH | fixed |
| E2 | All seven `updates:*` registrations were likewise ungated: a non-main frame could trigger a download and install (the app replaced on disk and restarted) or read the installed version. | MEDIUM | fixed |
| E3 | A packaged build honoured `--inspect`, `--remote-debugging-port` and `--remote-allow-origins`, exposing a DevTools endpoint on the process that holds the decrypted gateway key, the SQLite handle and the host bridge. | HIGH | fixed |
| E4 | The packaged renderer had no Content-Security-Policy at all. | HIGH | fixed |
| E5 | `host:save-bytes` passed the renderer-supplied filename straight to `dialog.showSaveDialog({ defaultPath })` unsanitised — a model-named path in the one field people accept without reading. | MEDIUM | fixed |
| E6 | `host:save-bytes` had no size cap. | LOW | fixed |
| E7 | `host:save-bytes` could throw, surfacing a rejected promise carrying a main-process path to the renderer. | LOW | fixed |
| E8 | "Start over" cleared storage only; the HTTP cache, cached HTTP auth and code caches survived the reset — and the comments claimed caches were dropped. A reset restarted onto a window still serving the previous install's bytes and credentials. | MEDIUM | fixed |
| E9 | The `agentforge://` path key was templated into `/api/v1/media/<key>/file` with no validation. | MEDIUM | fixed |
| E10 | The exit path spawned bare `taskkill`, so a `taskkill.exe` earlier on PATH would inherit this process's environment. | MEDIUM | fixed |
| E11 | `scripts/macos-app.mjs` launched the packaged `.app` with `--remote-debugging-port=9222`. | LOW | fixed |

**Shell review, not fixed**

| # | Finding | Severity | State |
|---|---|---|---|
| E12 | electron-builder 25.1.8 has no `electronFuses` support, so the `runAsNode` / inspect / cookie-encryption / asar fuses are unavailable until the 26.x bump. | MEDIUM | backlog |
| E13 | `webRequest.onHeadersReceived` never reaches the `file://` renderer — Electron's `webRequest` does not observe Chromium's file loader. Anyone "adding a CSP via the header" and stopping there ships no policy at all; the meta tag is the load-bearing half. | INFO | documented (both halves shipped) |
| E14 | `apps/desktop/splash/index.html` is a second `file://` document loaded into the same window and has no CSP. Static and local, so low risk, but it is the one uncovered document left in the shell. | LOW | backlog |
| E15 | The CSP has not been smoke-tested against a real packed build. The policy itself was proven on a real `file://` Electron window with a Vite-shaped page; nobody has run `desktop-build` and launched `win-unpacked` since. | MEDIUM | owed before the next release |
| E16 | `apps/web/lib/tool-media.ts` and `composer-attach.ts` accept remote `http(s)` media URLs from tool output, which `img-src` / `media-src` now block in packaged builds. Nothing remote reaches them today; this should be a decision, not a surprise bug report. | LOW | documented |
| E17 | `sandbox: false`, forced by `preload.cjs` requiring `brand-read.cjs` (fs + `process.resourcesPath` for brand and logo). | MEDIUM | backlog |
| E18 | No `setPermissionRequestHandler` and no `setPermissionCheckHandler`: camera, mic, geolocation and notification requests fall through to Chromium defaults. | MEDIUM | backlog |
| E19 | `hardenNavigation()` is applied only to the main window inside `createWindow()`; there is no `app.on("web-contents-created")` catch-all, so any future webContents gets no navigation or window-open rules. | LOW-MEDIUM | backlog |
| E20 | `host:ping` is the one channel left ungated. It returns a constant `{ ok: true }` and leaks nothing. | INFO | accepted as-is |

**Packaging observations (config reads, not a packaging review)**

| # | Finding | Severity | State |
|---|---|---|---|
| E21 | The Windows NSIS build is unsigned — no `certificateFile`, no `sign`, no `afterSign` hook. | HIGH | backlog |
| E22 | `build.mac.identity` is `null`; the dmg/zip are unsigned, which is why auto-update already disables itself on darwin. | HIGH | backlog |
| E23 | `nsis.deleteAppDataOnUninstall: true` — uninstall destroys the owner's desk, `settings.enc` and all media. Defensible, but it is a product decision worth stating out loud. | MEDIUM | product decision |
| E24 | `migrateLegacyUserData` copies the entire legacy userData directory (`cpSync(..., { recursive: true })`). | LOW | backlog |
| E25 | The secrets wrap key is stored via keytar rather than Electron `safeStorage`. | LOW | backlog |

On **E12**: the installed electron-builder is **25.1.8**, which has no `electronFuses` key — it is absent from `app-builder-lib@25.1.8`'s `scheme.json`, so adding it would be a silent no-op or a schema failure. The key arrived in electron-builder 26.x, so this waits on that major bump, done and packed in the isolated NSIS worktree. When it lands, `enableEmbeddedAsarIntegrityValidation` is the one flag to leave `false`: it needs the asar header hash embedded in a **signed** binary, and this Windows NSIS flow is unsigned (`mac.identity` is `null` too).

On **E4 / E13**: the policy lives in one place, `apps/desktop/renderer-csp.cjs`, and reaches the app twice — a `<meta http-equiv>` injected into `resources/renderer/index.html` by the pack step (`scripts/stage-renderer.mjs`), and `session.defaultSession.webRequest.onHeadersReceived` installed only when `app.isPackaged`. The meta tag is what actually enforces it, because `webRequest` does not observe Chromium's `file://` loader. `apps/web/index.html` is deliberately untouched: Vite's React plugin injects an inline Fast Refresh preamble, the HMR client uses `eval`, and it opens a websocket to `127.0.0.1:3000` — a meta policy there would break webdev and add nothing to the shipped build.

**Backlog (not fixed on this branch).** The first two and the last four were *not* reviewed in this pass — they are carried here as known work, not as findings of it. What this pass did confirm is E21/E22, the unsigned precondition that drives the `enableEmbeddedAsarIntegrityValidation` conclusion above.

- **Authenticode signing for the Windows installer** — procurement, and by some distance the highest-value item on this list.
- Apple Developer ID + notarization for the macOS cut.
- Electron major upgrade, then `sandbox: true` on the renderer — which first needs brand/logo loading moved off the preload.
- `setPermissionRequestHandler` / `setPermissionCheckHandler` on the session (deny by default).
- `app.on("web-contents-created")` hardening for any future webview or child window.
- Checksum pins for the `rcodesign` and `keytar` downloads, and migrating keytar to Electron `safeStorage`.
- An explicit `perMachine` decision for the NSIS target.
- Narrow the legacy userData migration further.
- Release hygiene: scope the publish PAT, require 2FA, protect the release tags, and put SHA256 sums in the release notes.

#### (d) Renderer, gateway client, cloud/portal, repo hygiene — reviewed 2026-09-15

Eleven findings. Eight fixed on this branch (written up as "Renderer media lock-down" in [`0.14.26-changelog.md`](0.14.26-changelog.md)), three open as repo/dev-server hygiene.

**Fixed this pass**

| # | Finding | Severity | State |
|---|---|---|---|
| d1 | **Model output could make the renderer fetch a chosen origin.** A remote `![alt](https://…)` in tool or model output became an auto-loading `<img>` / `<video>`, so the mere act of rendering an answer beaconed to whatever host the model named — with the viewer's IP, and on a packaged build from inside the app. | HIGH | fixed — `isRenderableImageUrl` / `isRenderableVideoUrl` now live in `apps/web/lib/renderable-media.ts` and accept **only host-served media** (`/api/v1/media/`, `agentforge://media/`) plus `data:image/{png,jpeg,webp,gif}`. `composer-attach.ts` re-exports them; `parse-markdown.ts`'s `safeImageSrc` and `tool-media.ts` use them, so a remote image degrades to a plain `safeLinkHref` link. |
| d2 | **The host media mirror was an SSRF sink.** `saveGeneratedImage` / `saveGeneratedVideo` fetched whatever URL came back from a generation — including a plain `http://` video branch — with no size cap, no redirect re-check and no address filtering, and handed back someone else's URL when the mirror failed. | HIGH | fixed — every remote URL routes through the new `packages/host/src/media-download.ts`, which uses `fetchPublicHttps` with 10 MB / 50 MB caps: HTTPS only, every redirect hop re-checked, a timeout, and a throw rather than a foreign URL handed back. `runs.ts` now mirrors generated **video** as well as image parts and drops a part it cannot mirror, so chat `video_generate` results still render. (Address-level filtering is still name-only — see b10.) |
| d3 | **Key-bearing requests followed redirects.** `gateway/account.ts` and `models/probe.ts` sent `Authorization: Bearer <gateway key>` with default redirect handling, so a 3xx from the endpoint forwarded the key to the redirect target. | MEDIUM | fixed — both send `redirect: "manual"` and treat a 3xx as a redacted error; the probe also runs `assertAllowedEndpointUrl` first. |
| d4 | **CI workflows ran with the default write-scoped `GITHUB_TOKEN`.** | MEDIUM | fixed — `.github/workflows/e2e.yml` and `desktop-mac.yml` declare top-level `permissions: contents: read`. **`--ignore-scripts` was *not* added**: `better-sqlite3` needs its postinstall to build, so both installs stay as they are. That remains an accepted risk, not an oversight. |
| d5 | Research source links rendered an unvalidated href from model output. | LOW | fixed — `research-preview.tsx` sends source links through `safeLinkHref`. |
| d6 | `ux-verify-live.mjs` built a shell command by interpolation and ran it through `execSync`. | LOW | fixed — `execFileSync("node", ["-e", …, file])`, no shell. |
| d7 | `data:` image handling was broader than it needed to be (any `data:image/*`, including SVG, which carries script). | LOW | fixed — narrowed to `data:image/{png,jpeg,webp,gif}`; SVG no longer renders from a data URI. |
| d8 | The model-probe failure message echoed the full endpoint URL including its query string, which can carry a key or token. | LOW | fixed — the probe strips the query string out of its failure message. |

**Open — repo and dev-server hygiene**

| # | Finding | Severity | State |
|---|---|---|---|
| d9 | `.gitignore` does not cover `.env*`. `data/` and the generated desk paths are ignored (a3, fixed), but a `.env.local` dropped at the repo root by any tool is committable. No such file exists today and none has ever been committed. | LOW | **open** — add `.env*` (with an `!.env.example` escape if one is ever wanted). |
| d10 | No retention policy on `refs/t3/*` and the checkpoint refs. They accumulate working-tree snapshots from agent sessions indefinitely, and a snapshot taken before a `.gitignore` tightened is exactly where a stray artifact would survive. | LOW | **open** — decide a retention window and prune; audit the existing refs once before the first prune. |
| d11 | The webdev express server sets no security headers of its own (no `nosniff`, no frame-ancestors, no referrer policy) on non-`/api` responses. The host adapter now stamps `nosniff` on its own responses (b6) and the packaged renderer has a CSP (E4), so this is the dev server only. | LOW | **open** — a small header middleware in `apps/web/server.ts`; deliberately not a meta CSP in `apps/web/index.html`, which would break Fast Refresh (see E4/E13). |

**Confirmed clean in this pass, and worth recording because each is a question that will be asked again:**

- **No telemetry of any kind.** No analytics SDK, no crash reporter, no phone-home on boot or update check beyond the explicit GitHub release query and the owner's own gateway calls.
- **No key ever committed in any ref.** History and every `refs/t3/*` snapshot scanned for key-shaped strings: none. The test key exists only in a throwaway desk's `settings.enc` (S6).
- **No HTML sinks in the renderer.** No `dangerouslySetInnerHTML`, no `innerHTML` assignment, no `document.write`; Markdown goes through the parser, never through an HTML pass.
- **XLSX / OOXML generation is inert.** No formula injection surface: written cells are typed values, and nothing user-supplied is emitted as a formula.

## 4. Process / environment

| # | Item |
|---|---|
| E1 | A stale throwaway webdev (`tsx server.ts` child) is still on port 3000; the permission classifier denies `taskkill` / `Stop-Process` for the orchestrator. Stopping a background pnpm shell never frees the port; drivers start their own instance on `PORT=3100` instead. Kill the 3000 process by hand when convenient. |
| E2 | One worker ran `git stash -u` mid-run with other workers in the tree; recovered, but do not repeat. Branch and commit soon: 163 changed files uncommitted is the biggest single risk on this machine. |
| E3 | `pnpm` is not on PATH; use `npx pnpm@9.15.9` or `corepack pnpm`. Run packages individually; never set a global `AGENTFORGE_DATA_DIR` for a full run (parallel workers race `ensureSchema`). |
| E4 | `tsc --noEmit` reports pre-existing errors in `edit/agent-run.ts`, `edit/backend.ts`, `handlers/agents.ts`, several `edit/*.test.ts`, `core/src/models/request-constraints.ts`, `usage-range-chart*.ts`. Vite and vitest do not typecheck, so these are invisible to `pnpm lint` / `test`. Worth a cleanup pass before they mask a real regression. |

## 5. Green at time of writing (2026-09-15, after the Finance fixes)

db 41 · core 1302 (+1 skipped) · host 675 · web 375 · desktop 6 suites · legal 1 · marketing 1 · university 3 · lint 0 errors (149 warnings pre-existing) · `apps/web` build ok · no `.env` / `settings.enc` / `.master-key` / `data/` in `git status`.

## Closed

### Product blockers P1–P6 — closed 2026-09-15

| # | Blocker | Closed by |
|---|---|---|
| P1 | Default job model (`deepseek-v4-flash`, the effective default for Finance / Documents / Market because `hy3` is not in the gateway catalog) sat silent 1 to 4+ min and ~50% of Finance generations timed out. The `thinking: { type: "disabled" }` knob is rejected by the gateway (HTTP 400, "Permintaan tidak valid", 5/5, also on `deepseek-v4-pro`). | **Closed.** Jobs send `{"reasoning_effort":"low"}` and nothing else (`packages/core/src/models/job-thinking.ts`, applied in `ai-sdk-runtime.ts` only when `execute` carries `jobMode`; Chat's body unchanged). Close condition was 4/5; the restarted 3100 desk returned **5/5 at 29–41 s**. No model default reordered; `EFFECTIVE_JOB_MODEL` in `mode-defaults.ts` now states in one place that `hy3` is absent from this gateway's catalog. |
| P2 | Stream watchdog first-token phase was no longer a hard cap: liveness-only frames (`step-start`, `response-metadata`, empty deltas) extended it forever, and the old 120 s failsafe in `runs.ts` had been removed. A heartbeating gateway could hang Chat indefinitely. | **Closed.** The hard cap is restored: one rule in `streamWatchdogDeadline`, used by both the checker and the armed timer, so a liveness-only frame can never push the first-token budget out. Chat's one-shot `setTimeout` is replaced by `armRunStallGuard` / `isRunOutputEvent` (`packages/host/src/run-stall.ts`), rearmed from `onEvent` with the model's own reasoning-aware limits. |
| P3 | Job runs never passed `locale` to `runtime.execute`, so timeout / contact-probe messages were English on an `id` desk. | **Closed.** Jobs pass `locale` into `runtime.execute`; `job-regen.test.ts` proves the run input carries `locale: "id"` under an id run context. **Passed.** |
| P4 | Upstream gateway error text reached the UI verbatim (Indonesian 400 body on an English desk). | **Closed.** 400 / 401 / 403 / 429 / 5xx are mapped to app-locale copy in `packages/core/src/gateway-http-copy.ts` (`gatewayHttpFailure`), status code kept in the headline, upstream sentence only in the log, and `detail.message` redacted on the way out. 402, 404 and the rest still pass through verbatim because `isMissingWireEndpoint` classifies on them. |
| P5 | Three code changes (P1 effort-only, P2, P4) were not live-verified; the 3100 desk executed pre-change host/core code (`tsx server.ts` has no watcher). | **Closed.** Live-verified on **round 4** after a restart of the webdev desk: the 5-attempt table (P1, 5/5), the Drive A run, and Chat sanity on the same model. |
| P6 | `enhance-prompt.ts:59`, `knowledge-map.ts:118,138` called `collectJobAssistantText` without `jobMode`, so they kept the hidden reasoning pass on quiet families. | **Closed.** Both pass a mode; every studio names its mode at its `collectJobAssistantText` call (finance, documents, presentations, research, data, market, legal), and so do Enhance prompt and the knowledge map. Changelog line updated. |
